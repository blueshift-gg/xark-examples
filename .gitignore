# Build artifacts (regenerate with `just prove` / `just export` / `cargo build-sbf`)
**/target/
*.gz

# Rust — commit lockfiles for reproducible example builds
Cargo.lock
!e2e/Cargo.lock
!01-over-9000/program/Cargo.lock
!02-age-verification/program/Cargo.lock
!03-shielded-pool/program/Cargo.lock

# Anchor
.anchor/
test-ledger/

# Keys & secrets — never commit
*-keypair.json
.env
