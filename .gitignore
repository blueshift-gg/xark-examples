# Build artifacts (regenerate with `just prove` / `just export` / `cargo build-sbf`)
**/target/
*.gz

# Rust
Cargo.lock
!e2e/Cargo.lock

# Anchor
.anchor/
test-ledger/

# Keys & secrets — never commit
*-keypair.json
.env
