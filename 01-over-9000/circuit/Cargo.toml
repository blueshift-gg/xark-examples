# The "over 9000" circuit — an ordinary Rust crate that xark compiles to R1CS.
[package]
name = "over_9000"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[workspace]

[lib]
path = "src/lib.rs"

[dependencies]
xark = { path = "../../../xark/crates/lang" }
