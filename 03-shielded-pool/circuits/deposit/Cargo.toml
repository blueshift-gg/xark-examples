# Shielded pool — deposit circuit crate (Rust → R1CS via xark).
[package]
name = "shielded_pool_deposit"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[workspace]

[lib]
path = "src/lib.rs"

[dependencies]
xark = { path = "../../../../xark/crates/lang" }
pool-merkle = { path = "../merkle" }
