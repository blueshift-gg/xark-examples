# Host-side builds, formatting, linting, and LiteSVM tests use this exact stable
# toolchain. xark invokes its own pinned nightly; cargo-build-sbf invokes its
# pinned Solana platform toolchain.
[toolchain]
channel = "1.95.0"
profile = "minimal"
components = ["clippy", "rustfmt"]
